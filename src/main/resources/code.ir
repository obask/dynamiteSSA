
(Prototype intToLong (# Int) -> Long)

(Prototype main1  (# ) -> VoidTy)
(DefDef main1 (#  )
    (Block
        (ValDef tt 123)

        (ValDef xxx (If tt
            (Block 43)
            Else
            (Block

            (If tt
                        (Block 666)
                        Else
                        (Block 666))
            )))

        (newString "helloDotty!" ) ) )
